accesscontrol:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/access_control
    username: root
    password: root
#   这里的密码以 0 开头可能会报错，这时需要将密码加上括号即可；